[alias]
  st = status
  pu = pull
  ci = commit
  ca = commit -a
  br = branch
  cho = checkout
  pom = push origin master
  hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
  type = cat-file -t
  dump = cat-file -p
