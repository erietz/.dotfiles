{
  "shebang": {
    "prefix": "shebang",
    "body":[
        "#!/usr/bin/env zsh",
        "$0"
    ],    
    "description": "#!/usr/bin/env zsh"
  },
  "gaussian-slurm": {
    "prefix": "gaussian-slurm",
    "body":[
        "#!/usr/bin/env zsh",
        "#SBATCH --nodes=1",
        "#SBATCH --ntasks-per-node=16",
        "#SBATCH --mem=10gb",
        "#SBATCH --time=${1:0-00:20:00}",
        "#SBATCH --job-name=${2:jobname}",
        "${3:#SBATCH --mail-user=ewrietz@iu.edu}",
        "${4:#SBATCH --mail-type=FAIL,BEGIN,END}",
        "",
        "cd $SLURM_SUBMIT_DIR",
        "g16 < ${5:filename}.com > $5.log",
        "",
        "$0",
        ""
    ],    
    "description": "slurm gaussian job"
  },
  "slurm": {
    "prefix": "slurm",
    "body":[
        "#!/usr/bin/env zsh",
        "#SBATCH --nodes=1",
        "#SBATCH --ntasks-per-node=48",
        "#SBATCH --mem=40gb",
        "#SBATCH --time=${1:0-00:20:00}",
        "#SBATCH --job-name=${2:jobname}",
        "${3:#SBATCH --mail-user=ewrietz@iu.edu}",
        "${4:#SBATCH --mail-type=FAIL,BEGIN,END}",
        "",
        "$0"
    ],    
    "description": "general slurm job"
  },
  "pbs": {
    "prefix": "pbs",
    "body":[
        "#!/usr/bin/env zsh",
        "#PBS -l nodes=1:ppn=16",
        "#PBS -l vmem=10gb",
        "#PBS -l walltime=${1:0-00:20:00}",
        "#PBS -N ${2:jobname}",
        "${3:#PBS -M ewrietz@iu.edu}",
        "${4:#PBS -m abe}",
        "",
        "g16 < ${5:filename}.com > $5.log",
        "",
        "$0"
    ],    
    "description": "pbs gaussian job"
  }
}
